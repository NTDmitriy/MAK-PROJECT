export type THeroBasic = {
  title: {
    text: string;
    accentText: string;
    accentOrder: number;
  };
  description: string;
};

export type THeroInfo = {
  title: string;
  description: string;
	image: string;
};

export type THowWorks = {
  title: string;
  description:string;
}