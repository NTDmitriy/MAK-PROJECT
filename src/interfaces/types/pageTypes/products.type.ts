export type THeroBasic = {
  title: {
    text: string;
    accentText: string;
    accentOrder: number;
  };
  description: string;
  img : string;
};

export type THeroInfo = {
  title: string;
  description: string;
  image: string;
};
